# The Water Conservation Garden -- thegarden.org, the org's real site
# (existing partners.json entry has previously pointed at a stale/wrong
# URL -- that partner-roster fix is issue 32's housekeeping list, out of
# this ticket's scope; this file registers a new *source* only).
# Live-verified 2026-08-30 (sprint 014 ticket 004 / issue 25):
#
#   GET https://www.thegarden.org/wp-json/tribe/events/v1/events/?per_page=1
#
# returns HTTP 200 with structured JSON, total=570 upcoming events.
#
# org_name matches partners.json's "The Water Conservation Garden" exactly.

org_name = "The Water Conservation Garden"
adapter_type = "tec_rest"
enabled = true

[config]
api_base = "https://www.thegarden.org/wp-json/tribe/events/v1/events/"
domain = "thegarden.org"

[acquisition_policy]
rate_limit_seconds = 1.0
respect_robots = true
discovered_via = "sprint 014 ticket 004 registration (live TEC REST verification, 2026-08-30, issue 25)"
