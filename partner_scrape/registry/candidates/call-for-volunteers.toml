# Candidate review stub -- NOT a live source.
#
# This file is deliberately missing adapter_type/config: it is
# never loaded by registry.loader.load_sources(). To promote this
# candidate, investigate its own site, add adapter_type/config,
# and move the completed file into registry/sources/.
org_name = "Call for volunteers"
candidate_url = "https://www.firstinspires.org/"
discovered_via = "sdcec-stem"
evidence_text = "FIRST Robotics - Call for volunteers - Sign up early to receive event volunteer training as inspectors, judges, referees or even scorers. Southern California regional events start in Winter."
