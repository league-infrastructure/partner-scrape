# Synthetic well-formed fixture for the company pipeline end-to-end test
# (ticket 006) -- not a real company's board. Exercises the Lever
# adapter (ticket 004) through a real pipeline.run() call, alongside
# greenhouse_fixtureco.toml, proving tickets 001-005 compose correctly
# end-to-end.

org_name = "Fixture Biotech Co"
adapter_type = "lever"
enabled = true

[config]
company = "fixturebiotech"
